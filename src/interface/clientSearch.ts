export interface CellPhoneSearchType {
  countryCode: string;
  phoneNumber: string;
}

export interface NameSearchType {
  firstName: string;
  surname: string;
  dob: string | null;
  sex: string;
}
